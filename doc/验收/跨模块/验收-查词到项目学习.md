# 跨模块验收：查词到项目学习

## 汇总表格

| 功能 | 场景 | 等级 | 验证方式 | 预期结果 |
| --- | --- | --- | --- | --- |
| 查词到理解 | 非首页搜索并阅读词条 | L2 | 真实 HTTP 浏览器流程 | 搜索命中词条，定义、边界、示例、来源可读 |
| 理解到练习 | 完成词条内小练 | L2 | 浏览器交互与 IndexedDB 读回 | 即时反馈正确，attempt 持久化 |
| 练习到路径 | 进入相关项目章节 | L2 | 普通链接导航 | 章节说明产物、检查点和关联词 |
| 恢复与携带 | 刷新、导出与重新导入 | L3 | 浏览器持久化流程 | 本地进度恢复，v2 导出可验证导入 |

## 详细报告

### 场景：从全局搜索完成一个项目学习闭环

1. 打开 `/{locale}/knowledge/`，用全局搜索输入一个规范名或本地标题。
2. 用键盘选择词条，确认本地标题、规范英文名、定义、边界、动态/静态示例和来源均可见。
3. 回答词条内练习并确认即时反馈；读取 `exerciseAttempts` 的对应稳定 ID。
4. 通过词条路径入口进入一个章节，核对章节产物、完成检查点与所有关联词链接。
5. 刷新页面确认记录仍在；导出 schema v2 JSON，验证结构后重新导入。
6. 在 390px 视口重复关键导航并确认无横向溢出、移动搜索关闭后焦点回到触发按钮。

预期：从查词、理解、练习到项目章节全部使用同一静态内容和本地状态；关闭账号、网络写入或 JavaScript 时，核心内容和普通链接仍可使用。

## 2026-08-16 修复后统一正式复验结果

| 环节 | 结果 | 实测证据 |
| --- | --- | --- |
| 非首页搜索到词条 | 通过 | 桌面与手机常驻搜索可用，键盘选择可进入 `api`/`authentication` 词条。 |
| 词条理解与四段示例 | 通过 | 情境、概念、项目、验证四阶段同时可见；切换后仅增加当前阶段高亮。 |
| 词条小练到本地记录 | 通过 | 即时反馈正确，`exerciseAttempts` 写入并可读回。 |
| 词条到项目章节 | 通过 | 路径章节普通链接可达，完成勾选写入 `pathProgress`，刷新后保持。 |
| 书签恢复 | 通过 | `bookmarks` 中 `selected: true` 写入后，刷新词条按钮 `aria-pressed` 仍为 `true`。 |
| 导出/导入/清空 | 通过 | 有效导入原子合并五个 store；无效行被完整拒绝且没有部分写入；清空移除 v2、legacy、fallback/v1 与迁移标记。 |
| 响应式闭环 | 通过 | 1280×720 首页三列；390×844 首页与词条无页面级横向溢出，搜索仍可见。 |

补充稳定性证据：三个修复定向 HTTP 3/3；导入用例连续压力复验 5/5；完整 HTTP 回归最终 10/10。完整回归首跑曾因测试固定等待 700ms 后过早读库得到 9/10；该测试已改为等待页面真实重载这一可观察完成条件，修改后再次连续通过 5/5。

本跨模块流程最终结论为：`通过`。

## 2026-08-16 六语种随机抽查复验

| 环节 | 步骤与预期 | 实际证据 | 结果 |
| --- | --- | --- | --- |
| 本地语言查词 | 在非首页输入本地词后提交，应进入准确词条并保留 canonical 英文名一次。 | Playwright CLI 在桌面 `zh-cn/terms/domain/` 的真实顶栏输入“威胁模型”并 Enter，进入 `/zh-cn/terms/threat-model/`；标题为“威胁模型 / Threat Model”，无重复 canonical。 | 通过 |
| 词条到理解/练习 | 六语种代表词条均应有定义、动态示例、练习、知识地图和项目路径入口，且移动可读。 | 固定种子 `20260816-vibe-terms-<locale>` 的代表页覆盖 zh-cn、zh-tw、ja、ko、de、ru；桌面与 390px 移动点检均确认上述入口可见、无横向溢出、控制台无错误。 | 通过 |
| 术语到路径的共享内容 | taxonomy、glossary、项目路径和词条内术语应受同一 locale 合同约束；移除语言不可留在路径。 | 43 项 Python 静态/内容门禁通过，含 taxonomy、glossary、path 和 Hindi 内容/键移除检查；项目路径保留 3 条、每条 10 章的现有静态合同。 | 通过 |
| 词义不串线 | 歧义词在查词、定义和项目语境应保持技术意义。 | 15 个高风险词在 6 语言共 90 个 title/short_definition 已抽查；修复复核后的 Shell、Canary-Release 和 multi-agent system 不再使用日常误译。 | 通过 |

本轮结论：`通过`。该结论为随机抽查式跨模块复验，不重复 500 词全量人工审校，也不覆盖发布后的线上状态。

## 2026-08-16 图解接入后的跨模块正式验收

### 汇总表格

| 环节 | 验证步骤与预期 | 实际证据 | 结果 |
| --- | --- | --- | --- |
| 查词到图解 | 从非首页使用顶栏搜索和键盘进入 `api`，定义后应出现定制图解。 | 连续真实浏览器流程到达 `/zh-cn/terms/api/`，图解 pattern 为 `request-response`，状态按钮可用 End 键切换。 | 通过 |
| 图解到练习 | 保留旧四阶段示例并完成词条练习，正确结果应写入本地 v2。 | 四阶段均保留；选择稳定 option id `definition` 后反馈为正确，完整浏览器套件同时读回 `exerciseAttempts`。 | 通过 |
| 书签刷新恢复 | 在同一词条加入书签并刷新。 | `api` 书签刷新后 `aria-pressed=true`；独立浏览器用例同时从 IndexedDB 读回选中记录。 | 通过 |
| 练习到项目路径 | 从词条内普通链接进入相关章节，完成后刷新。 | 从 `api` 进入 `/zh-cn/paths/ai-app/api-contract/`，勾选完成后刷新仍为 checked；完整套件同时读回 `pathProgress`。 | 通过 |
| 无 JS 与响应式 | 禁用 JS 后仍应可读定义、图解首状态/transcript、旧示例、练习、路径和来源；移动不得横溢。 | 六样本 × 两视口共 12 个无 JS 页面通过；根路径 20/20 与子路径 7/7 浏览器门禁无 console error、无页面横溢。 | 通过 |

### 详细报告

固定闭环为：`zh-cn/knowledge` 顶栏搜索 `API` → `zh-cn/terms/api` 的 request-response 图解 → 旧四阶段示例与词内练习 → `api` 书签刷新恢复 → 词条内项目链接 → `zh-cn/paths/ai-app/api-contract` 完成进度刷新恢复。该闭环在一个真实 Chrome context 内连续完成，未依赖账号、云端同步或服务器写入。

补充范围包括根路径浏览器套件 `20/20`、`BASE_PATH=/vibe-terms` 定向浏览器 `7/7`、固定六样本 48 个主题/视口交互组合和 12 个无 JS 页面。自动合同继续覆盖 IndexedDB 五 store、导入原子性、清空边界与普通链接。未发现跨模块回归。

### 本轮结论

`通过`

本结论针对当前本地候选和固定可复现抽样；未执行发布后线上读回，也不替代 500 词逐页人工视觉审查。
