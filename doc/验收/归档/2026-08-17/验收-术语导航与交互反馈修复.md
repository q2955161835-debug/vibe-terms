# 任务验收：术语导航与交互反馈修复

## 汇总表格

| 涉及模块 | 验收等级 | 验证结果 | 问题风险 | 最终结论 |
| --- | --- | --- | --- | --- |
| 知识地图分类栏、术语卡导航、词条页分页、示例卡交互、书签反馈与本地持久化、根/子路径适配 | L3 | 定向静态合同 `6/6`、JavaScript `17/17`；真实 Chrome 根路径 `4/4`、`BASE_PATH=/vibe-terms` `4/4`；补充浏览器探针全部通过 | 未发现 P0–P2；未执行全量 500 词人工检查、全盘回归、提交、合并、推送或部署 | 通过 |

## 详细报告

### 验收对象与范围

- 时间：`2026-08-17 00:43 ~ 2026-08-17 00:56`（Asia/Shanghai）。
- 对象：提交 `8fd00eb`，父提交 `1b864ba`；验收开始时位于 `codex/fix-term-navigation-interactions`，工作区干净。
- 范围只覆盖本次五项修复及其根路径/`BASE_PATH=/vibe-terms` 直接影响链；未扩展为全量 500 词人工检查或全盘回归。
- 使用本机真实 Chrome：`C:\Program Files\Google\Chrome\Application\chrome.exe`。补充探针以内存脚本运行，没有保存临时脚本或截图。

### 命令与结果

1. 差异与静态合同：

   ```text
   git diff --check 1b864ba 8fd00eb
   python -m pytest -q \
     tests/test_theme_contract.py::test_explorer_and_term_navigation_keep_visible_interaction_cues \
     tests/test_static_site.py::test_rich_term_page_is_readable_without_javascript \
     tests/test_static_site.py::test_home_is_a_clear_domain_topic_and_term_card_explorer \
     tests/test_static_site.py::test_static_example_pages_use_cards_as_progressive_controls \
     tests/test_static_site.py::test_root_build_has_complete_local_links \
     tests/test_static_site.py::test_project_base_path_applies_to_html_manifest_and_sitemap
   ```

   `git diff --check` 无输出；Python 定向合同 `6 passed in 47.97s`。

2. JavaScript 合同：

   ```text
   npm run test:js
   ```

   结果 `17/17` 通过，0 fail、0 skipped；覆盖核心本地状态、示例注册表和 Sites 路由合同。

3. 根路径真实 Chrome：

   ```text
   $env:PLAYWRIGHT_CHROMIUM_EXECUTABLE='C:\Program Files\Google\Chrome\Application\chrome.exe'
   python -m pytest -q `
     tests/test_browser.py::test_gateway_and_mobile_home_have_no_horizontal_overflow `
     tests/test_browser.py::test_dynamic_example_and_inline_exercise_persist_in_local_v2 `
     tests/test_browser.py::test_bookmark_selection_restores_after_reload `
     tests/test_browser.py::test_explorer_scroll_card_body_and_bottom_term_navigation
   ```

   结果 `4 passed in 33.85s`。

4. 子路径真实 Chrome：先设置 `$env:BASE_PATH='/vibe-terms'`，以上相同四项重新构建并执行，结果 `4 passed in 33.53s`。

5. 补充真实 Chrome 探针：以内存脚本分别构建根路径和 `/vibe-terms`，显式启动上述 Chrome，覆盖现有用例没有完全展开的暗色双视口、示例卡 Space、英文收藏及 pressed 样式。全部断言通过，进程退出码为 0。

### 分项验收证据

#### 分类栏与术语卡

- 在根路径和 `/vibe-terms` 的 `zh-cn` 首页，分别以暗色 `1280×720`、`390×844` 验证 `.explorer-tabs`。
- 桌面 `clientWidth=1198`、`scrollWidth=1565`，横向滚轮后 `scrollLeft=367`；手机 `clientWidth=390`、`scrollWidth=1597`，横向滚轮后 `scrollLeft=1207`。两种视口的 WebKit 滚动条均为 `display:block`、`height:8px`，标准属性为 `scrollbar-width:thin`；末项右边界均进入容器可视区。
- 点击术语卡正文实际进入该卡详情；根路径 href 为 `/zh-cn/...`，子路径 href 带 `/vibe-terms/zh-cn/...`。点击卡片书签后 URL 保持不变。

#### 词条底部分页

- `api` 词条在桌面下 prev 紧贴左侧、next 紧贴右侧，二者之间有正间距；分页区 `margin-top` 为 `72px`。
- `390×844` 下 prev/next 边界不重叠；首词条 `a-b-test` 只有 next 且靠右，末词条 `z-index` 只有 prev 且靠左。
- 上述分页场景在根路径和 `/vibe-terms` 各执行一次，均通过。

#### 示例卡与无 JavaScript 正文

- `api` 和 `css-custom-property` 的四张示例卡均以鼠标、Enter、Space 实际切换；每次切换后目标卡 `aria-pressed=true` 且成为 active。
- 禁用 JavaScript 后，以 `390×844` 打开两个页面，四张示例卡正文均可见且非空，`noscript` 说明可见，页面无横向溢出。

#### 收藏反馈与持久化

- `zh-cn/terms/api` 的按钮点击后立即由“收藏”变为“已收藏”，`aria-pressed=true`；IndexedDB `vibe-terms-local-v2/bookmarks` 中 `api.selected=true`，刷新后文案和 pressed 状态恢复。
- pressed 前后样式发生明显变化：文字 `rgb(18, 21, 28) → rgb(52, 88, 223)`，背景 `rgb(255, 255, 255) → rgb(238, 241, 255)`，边框 `rgb(226, 229, 235) → rgb(111, 140, 255)`。
- 英文抽样初始为 `Bookmark`，点击后立即为 `Bookmarked` 且 `aria-pressed=true`。

#### 诊断与主机边界

- 根路径与 `/vibe-terms` 的补充探针均收集 console warning/error 与 `pageerror`，结果为空。
- 所有补充页面均满足 `documentElement.scrollWidth <= clientWidth`；分类栏自身的预期横向滚动不会造成文档级横溢。
- 根路径和 `/vibe-terms` 的静态 URL 合同、卡片详情定向及相关交互均通过；本轮未执行线上主机读回。

### 问题与风险

- 未发现 P0–P2，也未发现本次范围内的 P3。
- 本结论是对提交 `8fd00eb` 的本地候选验收，不代表已合并 `main`、推送、部署或完成线上读回。
- 按最小充分原则，没有执行全量 500 词人工视觉检查、完整 Python/浏览器回归或 Sites 构建；这些不属于本次五项修复的验收范围。

### 最终结论

`通过`
