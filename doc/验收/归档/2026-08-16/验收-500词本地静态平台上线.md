# 任务验收：500 词本地静态平台上线

## 汇总表格

| 涉及模块 | 等级 | 验证结果 | 问题风险 | 详细报告 |
| --- | --- | --- | --- | --- |
| 内容、搜索、知识地图、项目路径、富词条、练习、本地数据、SEO、Pages | L3 | 通过 | 七种非英语内容仍为 `draft`/`noindex`，不声明人工审校完成 | 下方“详细报告” |

## 详细报告

- 候选身份：分支 `codex/feat-local-static-expansion`，验收开始时 `HEAD=2ba1296a344a40c6ac5a5ca510a4bd78372418ad`；VibeHub clarity 实现、测试、workflow、`design-qa.md`、`web/clarity.css` 等仍为未提交工作区变更，因此本结论针对该完整工作区候选，不把 `HEAD` 单独描述为已包含最终实现。
- 验收范围：VibeHub 清晰度重构；500 词、12 领域、42 主题、8 语言、三条 10 章项目路径、20 个注册动态示例与其余静态示例、每词练习、本地数据 v2、根/子路径、GitHub Pages workflow 和 Codex Sites 构建。

### 自动化命令与结果

- `python -m compileall -q scripts`、`python scripts/build_static_site.py`、`PLAYWRIGHT_CHROMIUM_EXECUTABLE=... python -m pytest -q --ignore=tests/test_browser.py`、三项 `node --check`、`npm run test:js`、`python scripts/package_site.py`：生成 8 语言、12 领域、42 主题、3 路径、500 词、4841 路由；Python 38/38、JavaScript 16/16 通过；静态归档生成成功。首次漏传指定 Chromium 路径时 35 项通过、3 项在浏览器启动前失败；修正环境变量后的完整 38 项全绿。
- `PLAYWRIGHT_CHROMIUM_EXECUTABLE=... python -m pytest -q tests/test_browser.py`：最终真实 HTTP 10/10 通过，覆盖搜索、主题、学习流、四段示例、练习记录、书签恢复、路径完成、严格/原子导入与彻底清空。
- `python -m pytest -q tests/test_urls.py tests/test_deploy_workflow.py tests/test_packaging.py`：9/9 通过，覆盖根路径、`/vibe-terms`、打包和 verifier-before-upload 合同。
- `npm ci && npm run build`：成功；`prepared 4885 Sites assets`，vinext 五阶段 build complete。
- `git diff --check`：无空白错误，仅报告 Windows 工作区 LF→CRLF 提示；`site/`、`dist/`、`node_modules/`、浏览器目录等未被 Git 跟踪。

### 视觉与交互证据

- 对照 2026-08-16 live VibeHub 与同视口截图。1280×720 首页：`innerWidth=scrollWidth=1280`，常驻搜索可见，12 个领域标签、4 个当前主题、55 张当前领域卡片，首个网格为三列且无控制台错误。
- 390×844 首页：`innerWidth=scrollWidth=390`，常驻搜索可见，领域/主题可横向浏览，卡片为单列且无控制台错误。
- 代表词条 `zh-cn/terms/api/`：桌面与手机均有四个可见示例阶段；点击“验证”后四段仍全部可见且仅 1 段为 active；桌面/手机均无页面级横向溢出。
- 非英文草稿继续标记 `draft` 并输出 `noindex`；不得把 4000 个语言文件描述成人工审校完成。

### 第一轮阻断的修复复验证据

1. **书签恢复通过。** 在 `zh-cn/terms/api/` 点击收藏后，IndexedDB `bookmarks` 存在 `{id: "api", selected: true}`；刷新后按钮 `aria-pressed=true`。
2. **导入校验与原子性通过。** 有效 schema v2 文件写入 `recentViews/imported-api`；包含无效 key 的文件显示完整 schema v2 校验消息，`termProgress/must-not-write` 不存在。实现先捕获 input 引用、逐行验证，再在五个 store 的同一 readwrite transaction 内合并。
3. **清空边界通过。** 预置 v2 五类记录、`vibe-terms-guest-v1` legacy 行、fallback/v1 与迁移标记后执行二次确认清空；刷新后 v2 五个 store 均为空，legacy 数据库和三类 localStorage 键均不存在。

定向证据为 core 11/11、三个 L3 HTTP 3/3。导入用例另连续压力复验 5/5；完整 HTTP 首跑曾因测试固定 `wait_for_timeout(700)` 后过早读库得到 9/10，随后完整 10/10。该用例已改为等待页面真实重载这一可观察完成条件，修改后又连续通过 5/5，不再保留固定 sleep 技术债。

### 上线与权威读回

- 任务分支已快进合并至 `main` 并删除；首个成功发布的站点源码提交为 `d172999b6dda83deaf50541b17e650a54de8922e`，远端 `refs/heads/main` 已读回一致。
- GitHub Pages 已创建为公开 workflow 模式并强制 HTTPS。Actions run `31934828326` 对提交 `d172999` 的 build 与 deploy 均为 `success`；完整 verifier、Pages 配置与 artifact 上传严格按顺序通过。
- 公开地址为 `https://q2955161835-debug.github.io/vibe-terms/`。根页、`zh-cn/`、`zh-cn/terms/api/`、`zh-cn/knowledge/`、`zh-cn/paths/personal-site/project-goal/`、`zh-cn/practice/` 与 `assets/clarity.css` 均读回 HTTP 200。
- 线上 DOM 读回确认：中文首页 canonical 正确、加载 `/vibe-terms/assets/clarity.css`、常驻搜索存在、当前领域 55 张卡；API 词条 canonical 正确且四阶段示例与练习存在；项目章节完成控件存在。1280×720 应用内浏览器读回无页面级横向溢出。
- 首次 run `31934180527` 在上传 artifact 前因渲染测试硬编码根路径失败；第二次 run `31934465773` 在上传前因 HTTP 测试服务器未挂载 `/vibe-terms` 失败。两次均未发布半成品；修正测试契约和子路径服务器后，第三次 run 完整通过并部署。

### 安全与变更边界

- 工作区模式检索未发现私钥、常见 GitHub/OpenAI/Google 密钥；仅 `.env.example` 被跟踪，`site/`、`dist/`、`node_modules/`、`.vinext/` 均未作为源码跟踪。
- 浏览器运行时只对同源静态 JSON 发起 `fetch`；未引入账号、Supabase/Firebase、分析、WebSocket、`eval`、`new Function` 或第三方 iframe。动态示例注册表测试确认内容不会被编译执行。
- GitHub workflow 权限限定为 `contents: read`、`pages: write`、`id-token: write`，完整 HTTP verifier 位于 Pages artifact 上传前。
- Codex Security 桌面 diff 扫描入口因工作区选择过期而返回“Repository HEAD changed after these working-tree changes were selected”，未产生 scan id；按插件规则没有另起替代扫描。这是工具覆盖缺口，不改变上述仓库内只读证据。

### 最终结论

`通过`

本地候选、合并后的 `main`、GitHub Actions 与公开 Pages 代表路由均完成权威读回。视觉、内容、搜索、四段示例、练习、书签与路径进度、本地导入/清空边界、主机适配、Pages workflow、Sites 构建与公开静态发布均通过。
