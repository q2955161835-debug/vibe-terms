# 暗色语言下拉框对比度修复验收

- 验收时段：`2026-08-17 00:16 ~ 2026-08-17 00:23`（Asia/Shanghai）
- 验收类型：任务验收
- 验收等级：L1（低风险样式修复）
- 验收分支：`codex/fix-dark-select-contrast`
- 被验收提交：`33aedea fix: improve dark locale option contrast`
- 基线提交：`fdc2c56 docs: record visual release readback`
- 验收角色：独立局部验收代理

## 汇总表

| 验收项 | 验证步骤 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| 变更范围 | 审查 `HEAD^..HEAD` 的文件、统计和完整差异 | 生产变更仅限语言 `option` 主题颜色，并包含对应回归测试；不改翻译/数据 | 通过：仅修改 `web/styles.css` 与 `tests/test_browser.py`，共新增 27 行 |
| 专项与回归门禁 | 运行专项暗色 option、theme cycle、system、search+locale 及 theme contract | 所有选定测试通过 | 通过：`6 passed in 28.23s` |
| 暗色选项可读性 | 本机 Chrome 在两档视口分别打开 dark、system-dark 下拉框，读取 7 个 `option` 的计算色 | 背景不透明，最低对比度不低于 4.5:1 | 通过：前景 `rgb(245, 247, 250)`，背景 `rgb(26, 35, 48)`，alpha=1，最低 `14.745:1` |
| 浅色回归 | 两档视口分别采样 light、system-light | 主题正确且 option 可读 | 通过：前景 `rgb(18, 21, 28)`，背景 `rgb(255, 255, 255)`，alpha=1，最低 `18.265:1` |
| 展开状态 | 在两档视口分别点击 dark、system-dark 的语言选择器并检查 `select:open` | 选择器进入真实展开状态并保持焦点 | 通过：4/4 样本 `:open=true`、`focused=true` |
| 响应式与运行时健康 | 检查 390×844、1280×720 的根节点/正文宽度、console 与 pageerror | 无横向溢出、无相关错误 | 通过：8/8 样本 `scrollWidth == clientWidth`，诊断数组为空 |
| 语言导航 | 在 1280×720 system-dark 下选择德语 | 从 `/en/` 导航到对应德语路由 | 通过：最终 URL 为 `/de/` |

## 详细报告

### 1. 差异审查

`git diff --stat HEAD^ HEAD` 与 `git diff --name-status HEAD^ HEAD` 显示：

- `web/styles.css`：为 `.locale-picker option` 增加 `color: var(--text-primary)` 与 `background: var(--panel-raised)`。
- `tests/test_browser.py`：新增 `test_dark_locale_picker_options_have_opaque_contrast`，验证暗色 option 背景为不透明 RGB 色且对比度至少 4.5:1。

没有翻译、术语、分类、路由数据或其他生产逻辑变更。修复范围最小，没有发现重复实现、临时补丁或新增技术债。

### 2. 自动化门禁

环境：

- 浏览器：`C:\Program Files\Google\Chrome\Application\chrome.exe`
- Playwright：项目 `requirements-dev.txt` 固定的 Python Playwright
- 站点：pytest 会话夹具生成 `site/` 并启动临时本地 HTTP 服务

命令：

```powershell
$env:PLAYWRIGHT_CHROMIUM_EXECUTABLE = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
python -m pytest -q tests/test_browser.py::test_dark_locale_picker_options_have_opaque_contrast tests/test_browser.py::test_theme_cycle_persists tests/test_browser.py::test_system_theme_respects_browser_color_scheme tests/test_browser.py::test_search_keyboard_and_locale_switch tests/test_theme_contract.py
```

结果：退出码 0，`6 passed in 28.23s`。

### 3. 真实浏览器取证

使用项目 Playwright 直接启动本机 Chrome（headless），临时 HTTP 服务器绑定 `127.0.0.1` 动态端口。每个样本均点击 `.locale-picker`，读取全部 7 个 `option` 的 `getComputedStyle()`，计算 WCAG 对比度与背景 alpha，并检查页面宽度、错误覆盖层、console warning/error 和 pageerror。测试完成后服务器线程均已关闭。

| 视口 | 模式 | `data-theme` / `color-scheme` | option 前景 / 背景 | 最低对比度 | 背景 alpha | 横向溢出 | 诊断 |
| --- | --- | --- | --- | ---: | ---: | --- | --- |
| 390×844 | dark | dark / dark | `rgb(245,247,250)` / `rgb(26,35,48)` | 14.745:1 | 1.0 | 无，390=390 | 空 |
| 390×844 | system-dark | system / dark | `rgb(245,247,250)` / `rgb(26,35,48)` | 14.745:1 | 1.0 | 无，390=390 | 空 |
| 1280×720 | dark | dark / dark | `rgb(245,247,250)` / `rgb(26,35,48)` | 14.745:1 | 1.0 | 无，1280=1280 | 空 |
| 1280×720 | system-dark | system / dark | `rgb(245,247,250)` / `rgb(26,35,48)` | 14.745:1 | 1.0 | 无，1280=1280 | 空 |
| 390×844 | light | light / light | `rgb(18,21,28)` / `rgb(255,255,255)` | 18.265:1 | 1.0 | 无，390=390 | 空 |
| 390×844 | system-light | system / light | `rgb(18,21,28)` / `rgb(255,255,255)` | 18.265:1 | 1.0 | 无，390=390 | 空 |
| 1280×720 | light | light / light | `rgb(18,21,28)` / `rgb(255,255,255)` | 18.265:1 | 1.0 | 无，1280=1280 | 空 |
| 1280×720 | system-light | system / light | `rgb(18,21,28)` / `rgb(255,255,255)` | 18.265:1 | 1.0 | 无，1280=1280 | 空 |

Chrome 支持 `select:open`；两档视口的 dark 与 system-dark 共 4 个展开样本均观测到 `:open=true`、`focused=true`。在 1280×720 system-dark 样本中选择 `de` 后，路由由 `/en/` 正常变为 `/de/`。

浏览器截图以字节流保留在内存中完成非空校验，未向工作区或工作区外写入截图文件。两次临时本地 HTTP 服务均已停止。

### 4. 问题、风险与边界

- P0-P2：未发现。
- 本次按要求仅覆盖 Windows 本机 Chrome/Chromium；未测试 Firefox、Safari、macOS 或 Linux 原生选择菜单。
- 未执行全量 pytest、`npm run build` 或全站回归；这是本次局部 L1 验收的明确范围边界。
- 原生菜单的像素级 OS 合成外观未做截图比对；已通过 Chrome 的真实 `select:open` 状态、全部 option 计算色、透明度、对比度及实际选择导航验证目标行为。

## 最终结论

**通过。** 当前提交满足暗色与 system-dark 语言下拉选项不透明、对比度至少 4.5:1 的要求；light/system-light、语言导航、两档视口布局与运行时健康未发现回归。允许进入分支合并前的后续流程，但本次验收代理未执行提交、合并、推送或部署。
